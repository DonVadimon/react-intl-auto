import any from 'any-module';

const Component = () => {
    return (
        <div>
            {intl.formatMessage({
                defaultMessage: 'hello',
            })}
        </div>
    );
};
